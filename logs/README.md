# /logs

**Status: NOT STARTED**

Observability output for Open Doors Financial OS v1 (Phase 13): every
major integration logs timestamp, event, contact/opportunity ID, source,
destination, result, error, and retry count — **never credentials**.

Actual runtime log files are git-ignored (see root `.gitignore`) — only
this README and, later, log-format documentation are committed here.
