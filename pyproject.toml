[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "csuite-revenue-os"
version = "0.1.0"
description = "White-label AI Revenue Operating System — C-Suite AI Agent System, built to direct GoHighLevel (GHL)."
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.setuptools.packages.find]
where = ["src"]
