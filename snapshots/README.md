# /snapshots

**Status: NOT STARTED**

The final snapshot/deployment package (Phase 15) that makes Open Doors
Financial OS v1 duplicatable into additional GHL subaccounts: a GHL
snapshot export (or documented manual-build steps where the public API
can't export one — see `docs/12-live-ghl-connection.md`'s confirmed
limitation that pipelines/workflows aren't API-creatable), paired with
`config/os-v1.yaml` and `config/ghl-object-registry.json` as the
configuration-driven source of truth a new deployment re-populates.

Blocked until Phases 1–14 are complete and tested.
