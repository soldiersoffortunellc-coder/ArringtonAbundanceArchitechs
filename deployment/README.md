# /deployment

**Status: NOT STARTED**

Will hold `NEW-LOCATION-CHECKLIST.md` and `DEPLOYMENT-MANIFEST.json`
(Phase 15) — the exact steps and manifest required to reproduce Open Doors
Financial OS v1 in another GHL subaccount. Blocked until the core system
(Phases 1–14) is built and tested against the first (Open Doors Financial
Group) location — duplicating an unproven system would just reproduce its
bugs.
